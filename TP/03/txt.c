1 funcao - recebe o array e seu tamanho e inicializa o contador em 0, o loop externo le todos os elemento, somando cada um deles ao total da soma

e ao final retorna o valor da soma de todos os elementos

2 funcao - declara os elementos da 1 funcao, calcula o tamanho o do array
e chama a 1 funcao passando o array declarado e o seu tamanho como parametros

imprimindo o resultado da soma com esses parametros

O (n) - o numero de operacoes é proporcional ao tamanho do array, ou seja, O (n)